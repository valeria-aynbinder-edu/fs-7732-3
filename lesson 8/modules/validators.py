def get_num_from_user() -> int:
    while True:
        num = input("Insert a num: ")
        if num.isdigit():
            return int(num)

DAYS_IN_YEAR = 365

def get_num(s: str) -> int:
    return int(s)
